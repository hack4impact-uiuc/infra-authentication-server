version: 2
jobs:
  backend_test:
      docker:
        - image: node:11.14.0
      dependencies:
        override:
          - echo $PATH
      steps:
        - checkout
        - run:
            name: checkout testing directory 
            command: cd test
        - run:
            name: install dependencies 
            command: yarn install
        - run:
            name: run tests
            command: yarn test
        
workflows:
  version: 2
  build-test:
    jobs:
      - backend_test
