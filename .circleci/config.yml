version: 2
jobs:
  backend_test:
      docker:
        - image: circleci/python:3.7
      steps:
        - checkout
        - run:
            command: |
              yarn install
              yarn test
workflows:
  version: 2
  build-test:
    jobs:
      - backend_test