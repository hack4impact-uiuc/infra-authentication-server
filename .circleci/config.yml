version: 2
jobs:
  backend_test:
      docker:
        - image: circleci/python:3.7
      dependencies:
        override:
          - echo $PATH
      steps:
        - checkout
        - run:
            name: install
            command: |
              cd test
              yarn install
              yarn test
        # - run:
        #     command: |
        #       yarn install
        #       yarn test
workflows:
  version: 2
  build-test:
    jobs:
      - backend_test